	1. function double(arr) { return arr.map(function(val) { return val * 2; }); }
	2. const double = arr => arr.map(val => val * 2);
	3. const squareAndFindEvens = numbers => numbers.map(val => val ** 2).filter(square => square % 2 === 0)
